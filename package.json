{
  "author": "MUmarShahbaz",
  "license": "Apache-2.0",

  "scripts": {
    "tailwind" : "npx @tailwindcss/cli -o ./assets/css/tailwind.css",
    "serve"    : "npm run tailwind && bundle exec jekyll serve",
    "build"    : "npm run tailwind && bundle exec jekyll build"
  },

  "dependencies": {
    "@tailwindcss/cli": "^4.1.17",
    "tailwindcss": "^4.1.17"
  }
}
